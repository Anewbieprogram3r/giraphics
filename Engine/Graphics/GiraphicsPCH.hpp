#pragma once

// Standard C++ Libraries
#include <array>            // For fixed-size array (std::array)
#include <cassert>          // For runtime assertions (assert)
#include <cstdlib>          // For general-purpose functions (std::exit, std::malloc, std::free)
#include <cstring>          // For C-style string manipulation (std::memcpy, std::strcmp)
#include <chrono>           // For timing
#include <fstream>          // For file stream operations (std::ifstream, std::ofstream)
#include <iomanip>          // For std::setw, std::left
#include <iostream>         // For input-output stream operations (std::cout, std::cin)
#include <limits>           // For numeric limits (std::numeric_limits)
#include <map>              // For sorted associative containers (std::map)
#include <memory>           // For smart pointers and memory management (std::unique_ptr, std::shared_ptr)
#include <set>              // For sorted set containers (std::set)
#include <stdexcept>        // For standard exceptions (std::runtime_error, std::invalid_argument)
#include <string>           // For string handling (std::string)
#include <unordered_map>    // For hash table-based associative containers (std::unordered_map)
#include <unordered_set>    // For hash table-based set containers (std::unordered_set)
#include <vector>           // For dynamic array (std::vector)
